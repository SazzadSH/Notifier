spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dshe_dms
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
notification:
  default:
    expiration: 3